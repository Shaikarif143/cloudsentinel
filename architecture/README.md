# Architecture assets

Drop the following here for the GitHub README / portfolio writeup:
- `aws-architecture.png` — exported from the ASCII diagram in the root README
- `terraform-module-graph.png` — output of `terraform graph | dot -Tpng`
- `lambda-flow.png` — scanner → compliance engine → remediation sequence
- `ci-cd-pipeline.png` — GitHub Actions job graph
- `dashboard-screenshot.png` — screenshot of the running React dashboard
- `demo.gif` — short screen capture of a live scan + dashboard update
